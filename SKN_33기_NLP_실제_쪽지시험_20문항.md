# SKN 33기 NLP 실제 쪽지시험 정리

- 문항 수: 20문항
- 형식: 객관식 4지선다형
- 용도: 실제 출제 문항 복습 및 시험 대비
- 정리 기준: 응시자 이름, 개인 점수, 응답 기록, Google 설문 안내 문구는 제외함

## 문제

### 1. 자연어 처리에서 토큰화(tokenization)의 결과를 가장 정확히 설명한 것은?

① 각 토큰에 중요도 순서대로 정수 번호를 부여한다.  
② 원문 문자열을 목적에 맞는 경계로 나누어 순서가 있는 문자열 토큰 목록을 만든다.  
③ 정수 ID를 학습 가능한 밀집 벡터로 바꾼다.  
④ 길이가 다른 정수 시퀀스에 PAD 값을 채운다.

### 2. 정제(cleansing)와 정규화(normalization) 구분에 맞는 설명은?

① `casefold()`는 정제이고, 이메일을 `__EMAIL__`로 바꾸는 것은 표제어 추출이다.  
② 두 처리 모두 문자열을 정수 ID로 바꾸는 토큰화이다.  
③ `Straße → strasse`는 정제이고, 이메일을 `__EMAIL__`로 바꾸는 것은 정규화이다.  
④ `Straße → strasse`는 표기 정규화이고, 이메일을 `__EMAIL__`로 바꾸는 것은 민감 정보 정제이다.

### 3. stemming(어간 추출)과 lemmatization(표제어 추출)의 차이에 대한 설명으로 옳은 것은?

① stemming은 문자열 규칙으로 어미를 잘라 비단어를 만들 수 있고, lemmatization은 품사와 사전을 이용해 `ran → run` 같은 불규칙 변화도 복원할 수 있다.  
② stemming은 품사 사전을 사용하고, lemmatization은 접미사만 기계적으로 자른다.  
③ 두 방법은 모든 단어에서 항상 같은 결과를 반환한다.  
④ lemmatization은 품사를 전달해도 불규칙 변화는 복원할 수 없다.

### 4. 정수 인코딩에서 PAD와 OOV의 역할을 올바르게 구분한 것은?

① OOV는 짧은 문장의 빈 위치를 채우고, PAD는 미등록 단어를 대신한다.  
② OOV는 미등록 토큰의 위치를 보존하는 ID이고, PAD는 길이를 맞추기 위해 내용이 없는 위치를 채우는 ID이다.  
③ 두 토큰은 같은 의미이므로 항상 같은 ID를 사용해야 한다.  
④ PAD는 토큰화 전에 원문의 공백을 모두 삭제하는 기호이다.

### 5. 어휘 크기가 7이고 토큰 ID가 2일 때 올바른 원-핫 벡터와 서로 다른 원-핫 벡터 사이의 거리 설명은?

① `[2, 0, 0, 0, 0, 0, 0]`이고, 거리는 ID 차이와 같다.  
② `[0, 0, 1, 0, 0, 0, 0]`이고, 의미가 비슷할수록 거리가 0에 가까워진다.  
③ `[0, 1, 0, 0, 0, 0, 0]`이고, 모든 거리는 1이다.  
④ `[0, 0, 1, 0, 0, 0, 0]`이고, 서로 다른 두 원-핫 벡터의 유클리드 거리는 항상 \(\sqrt{2}\)이다.

### 6. 문서 벡터 사이의 코사인 유사도에 대한 설명으로 가장 적절한 것은?

① 두 문장의 글자 수가 같으면 항상 1이다.  
② 벡터 크기의 합만 비교하므로 방향은 고려하지 않는다.  
③ 값이 높으면 두 문장의 의미가 완전히 동일하다는 사실이 증명된다.  
④ 두 벡터가 이루는 각도, 즉 방향의 유사성을 비교하며 높은 값만으로 의미의 완전한 동일성을 보장하지는 않는다.

### 7. `nn.Embedding()` 생성 시 입력 토큰 ID의 shape이 `(8, 3)`, `embedding_dim=4`, `padding_idx=0`일 때 옳은 설명은?

① PAD 위치가 삭제되므로 임베딩 출력은 곧바로 `(8, 4)`가 된다.  
② `padding_idx=0`만 설정하면 단순 평균의 분모에서도 PAD가 자동 제외된다.  
③ 임베딩 출력은 `(8, 3, 4)`이며, PAD가 문장 평균의 분자와 분모에 섞이지 않도록 별도의 마스크 평균을 적용한다.  
④ 토큰 ID의 숫자 크기가 임베딩 가중치의 중요도를 뜻한다.

### 8. TF-IDF와 Word2Vec의 표현 특성을 올바르게 설명한 것은?

① TF-IDF와 Word2Vec은 모두 각 차원이 특정 단어에 직접 대응하는 희소·지역 표현이다.  
② TF-IDF는 일반적으로 희소·지역 표현이고, Word2Vec은 일반적으로 밀집·분산 표현이다.  
③ TF-IDF는 밀집·분산 표현이고, Word2Vec은 희소·지역 표현이다.  
④ TF-IDF가 실수값을 사용하므로 자동으로 분산 표현이 된다.

### 9. 기본 RNN이 토큰 순서를 반영하는 핵심 동작은?

① 모든 토큰을 독립적으로 처리한 뒤 단어 빈도만 더한다.  
② 토큰 ID가 큰 순서대로 입력을 재정렬한다.  
③ 첫 토큰만 처리하고 나머지는 PAD로 바꾼다.  
④ 현재 입력과 이전 타임 스텝의 은닉 상태를 함께 사용해 현재 은닉 상태를 반복 갱신한다.

### 10. `batch_first=True`인 한 층 단방향 `nn.RNN`에 입력 shape `(2, 4, 3)`을 넣고 `hidden_size=5`로 설정했다. 올바른 출력 shape는?

① `output=(4, 2, 5)`, `hidden=(2, 5)`  
② `output=(2, 4, 5)`, `hidden=(1, 2, 5)`  
③ `output=(2, 5)`, `hidden=(2, 4, 5)`  
④ `output=(2, 4, 3)`, `hidden=(1, 2, 3)`

### 11. LSTM이 기본 RNN의 장기 의존성 문제를 완화하기 위해 추가한 핵심 구조는?

① 문서-단어 행렬과 IDF  
② 문자 n-gram과 BPE 병합  
③ 셀 상태와 입력·망각·출력 게이트  
④ Global Max Pooling과 여러 합성곱 필터

### 12. PyTorch `nn.LSTM`의 반환값을 올바르게 설명한 것은?

① `output`과 `(h_n, c_n)`을 반환하며, `output`은 모든 위치의 은닉 출력이고 `h_n`, `c_n`은 층×방향별 최종 은닉 상태와 셀 상태이다.  
② `output` 하나만 반환하며 셀 상태는 내부에서 항상 삭제된다.  
③ `h_n`은 전체 시퀀스의 원-핫 벡터이고 `c_n`은 정답 레이블이다.  
④ `output`은 확률 합이 1인 클래스 확률이고 `(h_n, c_n)`은 손실값이다.

### 13. 같은 입력 크기와 은닉 크기를 사용할 때 GRU와 LSTM의 구조 차이에 대한 설명으로 옳은 것은?

① GRU는 입력·망각·출력 게이트와 별도 셀 상태를 모두 사용한다.  
② LSTM은 게이트가 없고 은닉 상태 하나만 반복 갱신한다.  
③ GRU와 LSTM은 파라미터 수와 반환 상태가 항상 완전히 같다.  
④ GRU는 별도 셀 상태 없이 업데이트·리셋 게이트와 은닉 상태를 사용하므로 일반적으로 같은 크기의 LSTM보다 파라미터가 적다.

### 14. 세 뉴스 주제 중 하나를 예측하는 LSTM 분류기에서 `CrossEntropyLoss`에 전달할 값으로 옳은 것은?

① sigmoid를 적용한 `(B, 3)` 값과 `(B, 3)` multi-hot 정답  
② softmax를 미리 적용한 확률과 float형 `(B,)` 클래스 ID  
③ 마지막 은닉 상태와 원문 문자열  
④ softmax 전 `(B, 3)` 로짓과 long형 `(B,)` 클래스 ID

### 15. `[판타지, 모험, 로맨스]` 순서에서 판타지와 모험을 함께 가진 문서를 처리하는 방법으로 옳은 것은?

① 정답을 `[1, 1, 0]`으로 표현하고 레이블별 독립 확률에 임계값을 적용하며, 임계값은 validation에서 선택한다.  
② softmax 확률의 합을 1로 만든 뒤 argmax 레이블 하나만 고른다.  
③ 정답을 단일 클래스 ID 0으로만 표현해야 한다.  
④ test 점수가 가장 높아지는 임계값을 반복 탐색해 최종 임계값으로 정한다.

### 16. 길이 8, 임베딩 차원 24, 필터 16개, 커널 2·3·4를 사용하는 TextCNN의 올바른 흐름은?

① `(B, 8, 24)`를 전치 없이 Conv1d에 넣고 클래스 축을 pooling한다.  
② `(B, 8) → 임베딩 (B, 8, 24) → 전치 (B, 24, 8) → 커널별 Global Max Pooling (B, 16) → 결합 (B, 48) → 로짓`이다.  
③ 각 토큰을 LSTM에 순서대로 넣고 마지막 hidden만 사용한다.  
④ Global Max Pooling 뒤에도 각 필터가 반응한 정확한 토큰 위치 8개가 모두 유지된다.

### 17. 단어 단위 토큰화 대신 서브워드 토큰화를 사용하는 주된 이유는?

① 모든 문장을 항상 한 개의 토큰으로 만들기 위해서이다.  
② 단어 순서를 제거해 BoW와 동일하게 만들기 위해서이다.  
③ 자주 재사용되는 작은 조각으로 단어를 표현해 미등록 단어(OOV)를 줄이고 어휘 크기와 분할 길이 사이의 균형을 맞추기 위해서이다.  
④ 토큰 ID가 클수록 중요한 단어가 되도록 정렬하기 위해서이다.

### 18. SentencePiece 결과의 `▁`와 토큰 ID를 올바르게 해석한 것은?

① `▁`는 원문의 공백 경계·단어 시작 위치를 나타내고, ID는 현재 어휘에서 해당 조각을 찾기 위한 번호이다.  
② `▁`는 OOV가 발생했다는 표시이고, ID가 클수록 중요도가 높다.  
③ `▁`는 문장 끝을 뜻하며 디코딩할 때 항상 삭제되어 공백을 복원할 수 없다.  
④ 각 SentencePiece 조각은 반드시 언어학적 형태소 하나와 일치한다.

### 19. `영화가 재미있다`가 `['영화', '##가', '재미있', '##다']`로 나뉘었을 때 `##`의 의미는?

① 뒤 조각이 문장 전체를 대표하는 `[CLS]` 토큰이라는 뜻이다.  
② 공백 뒤의 새 단어가 아니라 앞 조각과 같은 원래 단어 안에서 이어지는 조각이라는 뜻이다.  
③ 해당 조각이 OOV라서 모델 입력에서 삭제된다는 뜻이다.  
④ 조각 뒤 숫자가 단어 중요도라는 뜻이다.

### 20. 언어 모델이 다음 토큰 예측을 반복해 문장을 생성하는 과정으로 옳은 것은?

① 각 단계에서 문장 전체 후보를 모두 완성한 뒤 가장 긴 문장을 고른다.  
② 첫 단계에서 만든 확률 분포를 고정한 채 여러 토큰을 연속 선택한다.  
③ 현재 문맥에서 토큰 하나를 선택해 문맥 뒤에 붙이고, 갱신된 문맥으로 다음 토큰을 다시 예측한다.  
④ 매 단계 반드시 greedy로 최댓값 토큰을 선택해야만 자동회귀 생성이다.

---

## 정답표

| 문항 | 정답 | 문항 | 정답 | 문항 | 정답 | 문항 | 정답 |
|---:|:---:|---:|:---:|---:|:---:|---:|:---:|
| 1 | ② | 6 | ④ | 11 | ③ | 16 | ② |
| 2 | ④ | 7 | ③ | 12 | ① | 17 | ③ |
| 3 | ① | 8 | ② | 13 | ④ | 18 | ① |
| 4 | ② | 9 | ④ | 14 | ④ | 19 | ② |
| 5 | ④ | 10 | ② | 15 | ① | 20 | ③ |

## 정답 및 해설

1. **②** — 토큰화는 원문을 문장·단어·형태소·서브워드 등의 분석 단위로 나누는 단계다. 정수 ID 변환, 임베딩, 패딩은 이후 단계다.
2. **④** — 정규화는 같은 의미로 취급할 표기 변형을 기준형으로 합치고, 정제는 노이즈·민감 정보처럼 그대로 쓰기 어려운 내용을 제거하거나 대체한다.
3. **①** — stemming은 규칙 기반 절단이므로 실제 단어가 아닌 결과가 생길 수 있다. lemmatization은 품사와 사전을 이용해 불규칙 활용도 복원할 수 있다.
4. **②** — OOV는 어휘에 없는 실제 입력 토큰을 대신하고, PAD는 배치의 길이를 맞추는 빈 자리다. 역할이 다르므로 ID와 마스크 규칙도 구분한다.
5. **④** — 0부터 시작하는 ID 2의 원-핫 벡터는 세 번째 원소만 1이다. 서로 다른 원-핫 벡터는 한 위치에서 \(1 \to 0\), 다른 위치에서 \(0 \to 1\)이므로 거리는 \(\sqrt{2}\)다.
6. **④** — 코사인 유사도는 벡터의 방향을 비교해 전체 크기 차이의 영향을 줄인다. 다만 벡터화 방식의 한계 때문에 높은 값이 의미의 완전한 동일성을 보장하지는 않는다.
7. **③** — `nn.Embedding`은 배치와 토큰 위치 축을 유지하고 마지막에 임베딩 축을 추가한다. PAD 행이 0이어도 전체 길이로 나누면 평균이 작아지므로 실제 토큰 수를 이용한 마스크 평균이 필요하다.
8. **②** — TF-IDF의 각 열은 실제 어휘에 대응하고 대부분의 값이 0인 경우가 많다. Word2Vec은 의미 정보를 여러 잠재 차원에 분산한 밀집 벡터다.
9. **④** — RNN은 현재 입력과 이전 은닉 상태를 함께 사용하고, 갱신된 은닉 상태를 다음 위치로 전달해 입력 순서를 반영한다.
10. **②** — `batch_first=True`이면 `output`은 `(batch, sequence, hidden)`이다. 최종 은닉 상태는 `(layers×directions, batch, hidden)`이므로 `(1, 2, 5)`다.
11. **③** — LSTM은 셀 상태와 입력·망각·출력 게이트를 사용해 정보의 기록, 유지, 출력을 조절한다.
12. **①** — `output`은 모든 위치의 은닉 출력을 담고, `h_n`과 `c_n`은 각각 층×방향별 최종 은닉 상태와 셀 상태를 담는다.
13. **④** — GRU는 별도 셀 상태 없이 업데이트·리셋 게이트와 은닉 상태를 사용하므로 같은 크기의 LSTM보다 일반적으로 파라미터가 적다.
14. **④** — `CrossEntropyLoss`는 내부에서 log-softmax를 처리하므로 softmax 전 로짓을 전달한다. 단일 레이블 정답은 `long`형 클래스 ID 벡터로 전달한다.
15. **①** — 멀티레이블 문제는 multi-hot 정답과 레이블별 독립 확률을 사용한다. 분류 임계값은 test가 아닌 validation 데이터로 정한다.
16. **②** — PyTorch `Conv1d`는 임베딩 차원을 채널 축으로 받는다. 커널별 Global Max Pooling 결과 16개씩을 결합하면 총 48차원 표현이 된다.
17. **③** — 서브워드는 반복되는 작은 조각을 재사용하여 새로운 형태의 단어도 조합하고, OOV와 지나치게 큰 단어 어휘 문제를 줄인다.
18. **①** — SentencePiece의 `▁`는 공백 경계나 단어 시작을 나타낸다. 토큰 ID는 중요도 점수가 아니라 현재 어휘에서 조각을 조회하기 위한 번호다.
19. **②** — WordPiece의 `##`는 해당 조각이 독립된 새 단어가 아니라 앞 조각과 같은 원래 단어 안에서 이어짐을 나타낸다.
20. **③** — 자동회귀 생성은 현재 문맥에서 다음 토큰을 선택해 문맥에 추가하고, 갱신된 문맥으로 다시 다음 토큰을 예측하는 과정을 반복한다. 선택 방식은 greedy나 sampling 모두 가능하다.

## 출제 영역

- 1~4: 토큰화, 정제·정규화, 어간·표제어 추출, 정수 인코딩
- 5~8: 원-핫 인코딩, 코사인 유사도, 임베딩, TF-IDF·Word2Vec
- 9~13: RNN, LSTM, GRU와 텐서 shape
- 14~16: 단일·멀티레이블 분류와 TextCNN
- 17~19: 서브워드, SentencePiece, WordPiece
- 20: 자동회귀 언어 생성
